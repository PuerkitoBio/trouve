describe "Trouve.work" do
    it "works?" do
        # ch = Channel(String).new
        # outch = Channel(String).new
        # stop = Channel(Bool).new

        # spawn do
        #     Trouve.work(ch, outch, stop)
        # end

        # got = [] of String
        # ch.send "a"
        # got << outch.receive
        # ch.send "b"
        # got << outch.receive
        # stop.send true

        # got.should eq(%w(a b))
    end
end
